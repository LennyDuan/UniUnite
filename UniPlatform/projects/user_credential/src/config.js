const config = {
  userTableName: 'user.users',
  credentialTableName: 'user.credential',
  tableKey: 'account',
  dateFormat: 'YYYY-MM-DD HH:mm',
  expiredTime: 1,
};

module.exports = {
  config,
};
